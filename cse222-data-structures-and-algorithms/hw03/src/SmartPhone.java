public class SmartPhone extends ExtendedDevice {
    public SmartPhone(String name, double price, int quantity) {
        setCategory("Smart Phone");
        setName(name);
        setPrice(price);
        setQuantity(quantity);
    }
}
